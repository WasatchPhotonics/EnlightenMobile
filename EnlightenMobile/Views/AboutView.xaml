<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:vm="clr-namespace:EnlightenMobile.ViewModels"
             x:Class="EnlightenMobile.Views.AboutView"
             BackgroundColor="#333"
             Title="{Binding title}">

    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Resources>
       <ResourceDictionary>
           <Color x:Key="DarkGray">#333</Color>
           <Color x:Key="Teal">#27c0a1</Color>
       </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout BackgroundColor="{StaticResource DarkGray}" 
                 VerticalOptions="CenterAndExpand"
                 HorizontalOptions="FillAndExpand">
        <Image Source="enlightenLOGO.png" HeightRequest="100" HorizontalOptions="Fill" Margin="10, 30, 10, 30" />
        <Label FontSize="22" 
               TextColor="#eee" 
               HorizontalTextAlignment="Center"
               Text="{Binding version, StringFormat='ENLIGHTEN™ Mobile {0}'}" />
        <Button Margin="30,20,30,20" 
                Text="Learn more" 
                Command="{Binding OpenWebCommand}" 
                BackgroundColor="{StaticResource Teal}" 
                TextColor="White" />
    </StackLayout>
</ContentPage>
